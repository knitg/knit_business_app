import React from "react";
import { View, Text } from "native-base";
import { ScrollView } from "react-native-gesture-handler"; 

function UserRole(props) {
  return (
    <ScrollView>
      <Text>User Roles Coming sooon .....</Text>
    </ScrollView>
  );
}

export default UserRole;
