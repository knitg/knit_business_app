import React from "react";
import { View, Text } from "native-base";
import { ScrollView } from "react-native-gesture-handler"; 

function UserType(props) {
  return (
    <ScrollView>
      <Text>User Type Coming sooon .....</Text>
    </ScrollView>
  );
}

export default UserType;
